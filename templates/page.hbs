{{> head}}
{{> header}}

<div class="portfolio-content">
  <div class="jumbotron" style="background-color: {{this.logo.background}}">
    <div class="container">
      <h1 style="color: {{this.logo.colour}}">{{this.title}}</h1>
    </div>
  </div>
  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-md-8 col-md-offset-2">
        {{{contents}}}
      </div>
    </div>
  </div>
</div>

{{> footer}}
