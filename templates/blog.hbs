{{> head}}
{{> home-header}}

<article class="container">
  <ul>
    {{#each collections.posts}}
      <li>
        <h3>{{this.title}}</h3>
        <article>{{{this.excerpt}}}</article>
      </li>
    {{/each}}
  </ul>
</article>

{{> footer}}